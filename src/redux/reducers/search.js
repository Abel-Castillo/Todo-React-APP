// import todosDB from "../../DB/todos";

// const searchBar = (state = todosDB, action) => {
//   switch (action.type) {
//     case "SEARCH_WORD":
//       const todos = [...state];

//       return state.filter(todo => todo.task.includes(action.searchValue));
//     default:
//       return state;
//   }
// };

// export default searchBar;
